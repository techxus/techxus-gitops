apiVersion: v1
kind: Service
metadata:
  name: hello-api
  labels:
    app: hello-api
spec:
  type: ClusterIP
  selector:
    app: hello-api
  ports:
    - port: 80       # service port (what gateway uses)
      targetPort: 8080  # container port in Spring Boot
      protocol: TCP

